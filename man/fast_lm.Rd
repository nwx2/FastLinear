% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_lm.R
\name{fast_lm}
\alias{fast_lm}
\title{fast_lm}
\usage{
fast_lm(X, y)
}
\arguments{
\item{X}{A matrix or data frame of predictors.}

\item{y}{A numeric vector of response variable.}
}
\value{
An object of class \code{fastlm}, which is a list containing:
\item{coefficients}{Estimated regression coefficients.}
\item{fitted.values}{Predicted values.}
\item{residuals}{Residuals from the model.}
}
\description{
Fits a linear model using QR decomposition for improved computational efficiency.
}
\examples{
set.seed(123)
n <- 1000
p <- 10
X <- matrix(rnorm(n * p), ncol = p)
beta_true <- rnorm(p + 1)
y <- cbind(1, X) \%*\% beta_true + rnorm(n)
model_fast <- fast_lm(X, y)
}
